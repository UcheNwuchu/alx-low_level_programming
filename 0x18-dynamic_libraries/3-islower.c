#include "main.h"

/**
* _islower - A function to check for lowercase character
* @c: the char to check
* Return: 1 if c is lowercase, 0 otherwise
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
